<% layout('layouts/boilerplate')%>

    <body>
        <h1>All Campgrounds</h1>
        <p> <a href="/campgrounds/new">Create a New Campground</a></p>
        <ul>
            <% for (let campground of campgrounds) {%>
                <li>
                    <a href="/campgrounds/<%= campground.id %>">
                        <%= campground.title %>
                    </a>
                </li>

                <% }%>
        </ul>
    </body>

    </html>